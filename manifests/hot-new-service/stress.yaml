---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hot-new-service
  namespace: default
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: hot-new-service
    spec:
      containers:
      - name: stress
        image: progrium/stress
        env:
        - name: CPU
          value: "4"
        args:
          - --cpu
          - "$(CPU)"
      nodeSelector:
        beta.kubernetes.io/os: linux

